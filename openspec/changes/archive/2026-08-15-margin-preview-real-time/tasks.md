## 1. 验证信号连接完整性

- [x] 1.1 确认四个 spinbox 的 `valueChanged` 信号正确连接到 `_on_margin_changed`
- [x] 1.2 确认 `_on_margin_changed` 中调用 `_on_config_changed()` 触发预览更新

## 2. 验证预览实时刷新

- [x] 2.1 手动调整"上"边距输入框，右侧预览在 500ms 内更新
- [x] 2.2 手动调整"下"边距输入框，右侧预览在 500ms 内更新
- [x] 2.3 手动调整"左"边距输入框，右侧预览在 500ms 内更新
- [x] 2.4 手动调整"右"边距输入框，右侧预览在 500ms 内更新

## 3. 验证预设联动

- [x] 3.1 选择预设方案（窄边距/宽边距），四个输入框同步更新，预览同步刷新
- [x] 3.2 手动输入不匹配预设的值，预设下拉框显示"自定义"
- [x] 3.3 点击"恢复默认"按钮，四个输入框恢复默认值，预览同步刷新

## 4. 验证配置保存与加载

- [x] 4.1 调整边距后退出程序，重新启动后边距值保持
- [x] 4.2 预览在启动时使用已保存的边距配置渲染

## 5. Bug 修复

- [x] 5.1 修复 `_on_margin_changed` 中 `disconnect` 在递归调用时抛 `RuntimeError` 导致预览不更新的问题（`try-except` 包裹）